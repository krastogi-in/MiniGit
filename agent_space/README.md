# Agent space

Per-ticket scratch for the AI SDLC factory.

**Layout:** `agent_space/<ISSUE-KEY>/` with `ideas/`, `specs/`, `tasks/`, `reviews/` as needed.

**Not product docs** — people guides stay in `docs/ai-sdlc/`; MiniGit product code stays in `src/` / `tests/`.

Ignore local run dirs if desired (`agent_space/*/`); keep this README tracked.
